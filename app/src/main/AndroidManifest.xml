<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.protoshuttleapp">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/panther_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/panther_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProtoShuttleApp">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <!-- Launcher -->
        <activity
            android:name=".ui.RoleSelectActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Student Side -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="false" />

        <!-- Driver Side -->
        <activity
            android:name=".ui.driver.DriverMainActivity"
            android:exported="false" />

        <!-- Manager Side -->
        <activity
            android:name=".ui.manager.ManagerMainActivity"
            android:exported="false" />

    </application>
</manifest>